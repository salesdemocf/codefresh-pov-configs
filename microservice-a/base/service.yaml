apiVersion: v1
kind: Service
metadata:
  labels:
    app: microservice-a
  name: microservice-a
spec:
  ports:
    - name: '80'
      port: 80
      targetPort: 80
  selector:
    app: microservice-a
